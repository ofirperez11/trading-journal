# יומן מסחר — מסמך אפיון (SPEC)

גרסה 2.0 · עודכן 2026-06-21

> שינוי מרכזי מ‑v1: המוצר הוא **אתר/PWA רב‑משתמשים עם שרת ענן (Supabase)**, לא יומן מקומי בלבד. בסיס בנוי להיות **מוכן למכירה** בעתיד.

---

## 1. חזון

יומן מסחר **אינטואיטיבי וידידותי בסגנון TradeZella** לסוחרי יומן בפיוצ'רס.
- **אתר (web app) שמתנהג כאפליקציה (PWA)** — רץ על Mac, אייפון וכל מכשיר; מותקן מהדפדפן בלי App Store.
- **רב‑משתמשים** — המשתמש, השותפים, ובעתיד לקוחות. לכל משתמש הנתונים שלו, מופרדים ומאובטחים.
- **ענן** — נתונים נשמרים ב‑Supabase; סנכרון Mac↔️אייפון מובנה; עובד גם offline (cache מקומי).
- **חינמי להתחיל** — Supabase free tier. עלות מתחילה רק אם המוצר גדל/נמכר (החלטה עסקית עתידית).
- **בסיס מוכן למכירה** — ארכיטקטורה נקייה ורב‑דיירת (multi-tenant) מהיום הראשון, כדי שאפשר יהיה "להדליק" מכירה בהחלטה של רגע. *לא* בונים מערכת תשלומים/מנויים עכשיו.

### עקרונות מנחים
- **סטטיסטיקות יפות וברורות = עמוד השדרה.** הניתוח והגרפים הם לב חוויית המוצר — הכי ויזואלי, ברור ומלוטש שאפשר.
- **מהירות וזרימה** — להזין/לסקור עסקה בכמה שניות.
- **לא רק מספרים** — גם תיעוד רגשי, משמעת ולקחים.
- **חינמי קודם** — כל פיצ'ר מתחיל בגרסה ללא עלות, עם נתיב שדרוג ברור.

---

## 2. ניהול משתמשים ואבטחה

- **הרשמה/התחברות** (אימייל+סיסמה, ובהמשך אולי Google).
- כל הנתונים משויכים ל‑`user_id`. **Row Level Security (RLS)** ב‑Supabase מבטיח שכל משתמש רואה רק את שלו.
- מבנה רב‑דיירת מההתחלה → מוכן לצמיחה ולמכירה בלי שכתוב.
- תפקידים בסיסיים (משתמש רגיל / אדמין) — שלד לעתיד.
- *לא* כולל כרגע: תשלומים, תוכניות מנוי, חיוב. הארכיטקטורה ערוכה להוסיף אותם בקלות.

---

## 3. שתי הפיצ'רים המרכזיים

### פיצ'ר A — ניתוח עסקה מצילום מסך 📸→📊
מעלים תמונה של עסקה → היומן קורא ממנה את הנתונים → ממלא עסקה אוטומטית.

**זרימה:**
1. העלאת תמונה (גרירה / הדבקה / צילום).
2. ניתוח: חילוץ סימבול, כיוון, כניסה, יציאה, כמות, P&L, תאריך/שעה.
3. **מסך אישור** — מציג את כל הפרמטרים שזוהו, **כל שדה ניתן לעריכה**, לפני שמירה.
4. **בחירת שעה מדויקת** — צ'קבוקס/בורר שעה לאישור או תיקון השעה שזוהתה.
5. שמירה → העסקה נכנסת ליומן עם התמונה מצורפת.

**זמין גם בבק‑טסט וגם בלייב.**

**מימוש — שלבים:** v1 (חינם) = OCR בדפדפן (Tesseract.js). v2 (שדרוג) = Claude Vision API — מדויק יותר, עלות אגורות לתמונה, מתחלף בלי לשנות שאר המערכת.

### פיצ'ר B — תיעוד אוטומטי של מסחר לייב (Tradovate) ⚡
עסקאות אמת מ‑Tradovate נכנסות ליומן בלי הקלדה ידנית.

**מימוש — שלבים:** v1 (חינם) = ייבוא קובץ CSV של Tradovate (מאחד fills לעסקאות, מזהה חדשות). v2 (שדרוג) = משיכה אוטומטית מ‑Tradovate API (מותנה בגישת API).

---

## 4. מודל הנתונים

מבוסס על 289 העסקאות שכבר ייצאנו. כל טבלה כוללת `user_id` (להפרדה והגנת RLS).

**Trade:** id, user_id, account_id, date, symbol, market, side, status (WIN/LOSS/WASH), qty, entry, exit, target, stoploss, entry_total, exit_total, return_amount, return_percent, r_multiple, hold_time, confidence, tags[], notes, mood, discipline_score, executions[], images[].

**ישויות נוספות:** User · Account (ריבוי חשבונות לכל משתמש) · Tag/Setup · JournalEntry (תיעוד יומי) · Image (ב‑Supabase Storage).

---

## 5. מסכים

1. **התחברות/הרשמה** — כניסה למערכת.
2. **Dashboard** — סקירה ויזואלית: equity curve, P&L כולל, win rate, avg win/loss, profit factor, רצפים. *עדיפות עיצובית עליונה.*
3. **רשימת עסקאות** — טבלה מסוננת/ממוינת (תאריך, סימבול, תוצאה, חשבון, תגית).
4. **פרטי עסקה** — כל הנתונים + צילומי מסך גדולים + הערות + executions.
5. **הוספת/עריכת עסקה** — ידני + **זרימת ניתוח‑מתמונה עם מסך אישור** (פיצ'ר A).
6. **לוח שנה (Calendar)** — P&L לכל יום, חודש במבט‑על.
7. **אנליטיקה מעמיקה** ⭐ — פילוח לפי setup / שעה / יום בשבוע / סימבול; התפלגות R‑multiple; ניתוח לפי רמת ביטחון; drawdown; profit factor; expectancy. **הכי יפה וברור שאפשר.**
8. **יומן רגשי ומשמעת** — תיעוד יומי, מצב רוח, עמידה בכללים, לקחים.
9. **ייבוא** — Tradovate CSV (פיצ'ר B) + ייבוא הנתונים הקיימים.
10. **חשבונות** — ניהול ריבוי חשבונות.
11. **הגדרות + פרופיל** — מטבע, עמלות, סוגי שוק, גיבוי/ייצוא, פרטי משתמש.

---

## 6. גישה טכנית

| רכיב | בחירה | למה |
|---|---|---|
| מסגרת | React + TypeScript + Vite | מודרני, מהיר, קוד אחד לכל הפלטפורמות |
| PWA | כן | התקנה על Mac ואייפון בלי App Store |
| עיצוב | Tailwind CSS | UI נקי ומהיר בסגנון TradeZella |
| גרפים | ספריית charts מתקדמת | equity curve, התפלגויות — עדיפות עליונה לאיכות |
| **Backend** | **Supabase** | Auth + Postgres + Storage + RLS, free tier נדיב |
| אחסון תמונות | Supabase Storage | תמונות עסקה בענן |
| Offline cache | IndexedDB | עבודה גם בלי אינטרנט, סנכרון כשחוזר |
| OCR (v1) | Tesseract.js | ניתוח תמונה בדפדפן, אפס עלות |
| ייבוא | מפענח CSV של Tradovate | פיצ'ר B v1 |

**נתיבי שדרוג:** Claude Vision (פיצ'ר A v2) · Tradovate API (פיצ'ר B v2) · מערכת תשלומים/מנויים (כשעוברים למכירה).

---

## 7. נתוני התחלה

החשבון הראשי כבר מיוצא: **289 עסקאות + 271 צילומי מסך** (`data_export/`). ייובאו לחשבון המשתמש שלך ביומן.

---

## 8. שלבי בנייה (הצעה)

- **שלב 0:** שלד הפרויקט (React+Vite+Tailwind+PWA) + חיבור Supabase + מודל נתונים + RLS.
- **שלב 1:** הרשמה/התחברות + ניהול משתמשים בסיסי.
- **שלב 2:** ייבוא 289 העסקאות + רשימת עסקאות + פרטי עסקה (כולל תמונות).
- **שלב 3:** **Dashboard + אנליטיקה מעמיקה** — ההשקעה העיצובית העיקרית. ⭐
- **שלב 4:** הוספה/עריכה ידנית + ניהול תמונות + חשבונות.
- **שלב 5:** פיצ'ר A — ניתוח מתמונה (OCR) + מסך אישור.
- **שלב 6:** פיצ'ר B — ייבוא Tradovate CSV.
- **שלב 7:** לוח שנה + יומן רגשי + ליטוש PWA לאייפון.
- **עתיד:** Claude Vision · Tradovate API · מערכת מכירה/מנויים.

---

## 9. החלטות פתוחות / לבדיקה בהמשך

- גישת API ל‑Tradovate (לא בטוח שיש) → מתחילים מ‑CSV.
- שדרוג ל‑Claude Vision (עלות אגורות) → כשנרצה דיוק גבוה יותר.
- מערכת תשלומים/מנויים → רק כשמחליטים לעבור למכירה; הבסיס ערוך לזה.
